# pnpm — see docs/SECURITY-SUPPLY-CHAIN.md
auto-install-peers=true
audit-level=high
fund=false

# Expo/RN need lifecycle scripts; do not set ignore-scripts=true globally.
# Shorter paths for Android native builds on Windows (pnpm default is very deep).
node-linker=hoisted
